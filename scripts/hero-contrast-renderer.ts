// Test-only bundle entry: exercise the live renderer, never the legacy scene.
export { drawKnowledgeScene } from '../src/visuals/drawKnowledgeScene';
export { createKnowledgeScene } from '../src/visuals/knowledgeScene';
