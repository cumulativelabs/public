# Independent Retina mask release retest

**QA_VERDICT: PASS. QA_COMPLETE.** The narrow mask correction resolves the predecessor’s Retina contrast failure in this independent, bounded retest. No candidate source, tests, manifests, lockfiles, branches or existing evidence were modified. This is local QA acceptance, not remote handoff or release authorization.

Candidate `7a04b1a4d45f2cea525ffb5c70d4369d5021f9d1`, tree `78e1f6bbc7ae012d04d9f8f7c2aa6a73e6ae0a40`, parent/substantive commit `b624a80334aa4cecb1030aaee8ba52fd5d533cf8`. Substantive tree `bb9a0d5f6ed9959494e501df3be7e6df2fcc6a7a`, parent/failed candidate `97ea04ee7c1e80e5a047c278807b593131dbabd3`. Production reference: `5c254645479892fdeed1bff4799b7c89f375e810`.

**Independent contrast:** 26 fresh candidate states: 16 density states, eight fresh-page transitions, and two hosted DPR2 states. Chromium **151.0.7922.34** and WebKit **26.6**, normal motion, fonts loaded. The 1440×900 DPR2 pointer (120,711), center x150, upper position, and 1280×800 center x280 pass in both engines; DPR1, 1.25, 1.5 and bounded DPR3 spots also pass. All computed masks are `100% 100%`.

| Sampled target | Measurements | Lowest actual composited interior contrast | Required | Minimum interior pixels per measurement |
| --- | ---: | ---: | ---: | ---: |
| Body | 26 | 8.337:1 | 4.5:1 | 574 |
| Support | 26 | 7.260:1 | 4.5:1 | 90 |
| Large headline spans | 78 | 3.110:1 | 3:1 | 1,891 |

The reviewed [sampler](contrast.mjs) derives separate x/y coordinate scales from each actual PNG’s dimensions and CSS viewport extent. Paired text/background captures and a separately exposed gradient establish glyph coverage; acceptance above uses actual composited text pixels with ≥95% inferred coverage. Foreground/background ratios are retained separately. Callbacks freeze once after the interaction on each fresh page; no renderer or mask replacement is used for these measurements. [Summary and mapping](acceptance-summary.json), [density measurements/PNGs](density/), [transition measurements/PNGs](transitions/). Scroll, tablet→desktop, loaded-font reload, and Chromium CDP density transitions pass; maximum measured pointer mapping error is 0.00144 CSS px. This does not certify physical monitor switching or every font/zoom/animation state.

**Failure lineage retained:** [prior report](https://github.com/cumulativelabs/public/blob/e083c1f5c9e1ec78a53c45448bce8baede1b4390/qa/pr4-release-97ea04e-20260911/REPORT.md), [controller qualification](https://github.com/cumulativelabs/public/blob/e083c1f5c9e1ec78a53c45448bce8baede1b4390/qa/pr4-release-97ea04e-20260911/CONTROLLER_READBACK.md), [native confirmation](https://github.com/cumulativelabs/public/blob/e083c1f5c9e1ec78a53c45448bce8baede1b4390/qa/pr4-release-97ea04e-20260911/native-confirmation.json), and [handoff](https://github.com/cumulativelabs/public/blob/e083c1f5c9e1ec78a53c45448bce8baede1b4390/qa/pr4-release-97ea04e-20260911/HANDOFF.json). The preliminary eight-state sampler mixed CSS/device coordinates and is invalid for quantitative acceptance; the separate native production comparison supports the predecessor FAIL. This run additionally verified all 13 saved predecessor assets against that QA manifest and freshly reproduced its failure: Chromium body/support 1.846/1.479:1; WebKit 1.883/1.393:1. [Fresh negative controls](supplemental-results.json), [paired PNGs](supplemental/). The creator’s callback-frozen transition harness failure remains preserved in candidate evidence; neither that invalid harness nor creator pass claims substitute for this verdict.

**Reach, appearance and regression:** unchanged repository probes pass: [18 alignment/fallback cases](matrix/browser-results.json), [16 capability-policy cases](policy/results.json), [eight tether cases](tether/results.json), [six fan cases](fan/results.json). The ≤25px tip and original fan thresholds were not loosened. Twelve fresh DPR2 shown/hidden compositor pairs record 16 tips each, maximum distance 20.036 CSS px. [Visibility evidence](visibility/visibility.json) shows 9–13 distinct qualifying join samples, 8–16 gap samples and 4–10 samples near 32px from the pointer. The fixed near-path sample falls to 1–4 distinct filaments in the 1440 lower case; overlap and CTA occlusion limit interpretation. These counts are finite visibility samples, not proof of every strand along its entire path. Ordinary upper/center/lower screenshots in both engines were visually reviewed: lettering and buttons remain protected, with bright strands in surrounding whitespace, broad fan, C/core/rings and purple output preserved.

All seven viewports were checked: 1440×900, 1280×800, 1024×768, 768×1024, 430×932, 390×844, 320×568. No observed overflow, CTA obstruction, console/page/hydration errors or fallback regression. Reduced motion, touch/coarse pointer, no-JS/no-Canvas, hidden/offscreen pause and resize mapping pass. Eight mobile/tablet static screenshot pairs are byte-identical to the verified predecessor; hashes and four fresh examples are retained. Menu traversal, Escape restoration, [desktop CTA focus and #work navigation](cta-results.json) pass. **Inherited nonblocking nit:** WebKit reduced-motion menu entry leaves focus on BODY; subsequent traversal and Escape restoration pass, consistent with retained production QA. No navigation remediation was attempted.

**Build, identity and scope:** independent `npm ci --fetch-retries=0 --fetch-timeout=15000`, lint, typecheck, 34 tests/eight files, build, `validate:build` and diff check pass. [Build results](build-results.json), [logs](logs/). All 13 files in the independently generated dist file set match **https://5b0eeaaa.cumulative-labs.pages.dev/** byte for byte, with noindex on every response; the creator manifest was only a cross-check. Tested local candidate: `http://127.0.0.1:4337/`; verified predecessor: `http://127.0.0.1:4322/`. [Assets, hosted checks, static hashes and performance](supplemental-results.json). PR 4 was directly read as draft/open/unmerged at this candidate. [PR state](pr-status.json).

The two-second Chromium DPR2 active sample measured 35.632ms scripting/second, 60 tether draws/second, zero layout calls and zero observed layout shift; hidden/offscreen samples had zero draws. Other browser work overlapped this bounded headless run. No performance improvement, battery, field or physical-device claim is made. [Source/privacy review](source-review.json) confirms only standard/WebKit mask size/position/origin/repeat CSS changed from the failed candidate; component geometry, copy, dependencies and telemetry are unchanged. Targeted candidate text/privacy scanning found no findings. Sandbox server/browser failures and a direct HTTP-client 403 were environment/retrieval issues, resolved through authorized execution/browser access; [attempts](retrieval-attempts.json) and raw negative evidence are retained. No candidate defect or acceptance blocker remains within this scope.

**Next action:** owner persists the unchanged verdict/evidence to `qa/pr4-dpr-mask-retest-7a04b1a-20260911`, verifies exact remote commit/tree/report bytes, and registers/readbacks the handoff. Exclude `private/` from public packaging; disclosed path/ANSI log normalization is recorded in [PUBLICATION_NORMALIZATION.json](PUBLICATION_NORMALIZATION.json). Do not rerun substance merely to recover persistence.

**REMOTE_READBACK_STATUS=PENDING_OWNER_PERSISTENCE; CONTROLLER_REGISTRATION=PENDING. MERGE_STATE=NOT_MERGED; DEPLOYMENT_STATE=PRODUCTION_NOT_DEPLOYED; PUBLICATION_STATE=LOCAL_QA_EVIDENCE_PENDING_OWNER.**
